#include <stdio.h>

/* This is a comment. */
int main(int argc, char *arg[])
{
    int distance = 100;

    // this is also a comment
    printf("You are %s miles away.\n", distance);

    return 0;
}
